pipeline {
    agent any
    stages {
        stage("Check Branch Name") {
            steps {
                script {
                    def branchName = env.BRANCH_NAME
                    if (!(branchName == "master" || branchName.startsWith("feature/"))) {
                        echo "Warning: The branch name '${branchName}' seems inappropriate."
                    }
                }
            }
        }
        stage("Test") {
            when {
                not {
                    branch "master"
                }
                anyOf {
                    branch "feature/*"
                    branch "bugfix/*"
                }
            }
            steps {
                echo "Test stage."
            }
        }
        stage("Fail") {
            when {
                not {
                    anyOf {
                        branch "feature/*"
                        branch "bugfix/*"
                    }
                }
            }
            steps {
                error("Wrong name for: ${env.BRANCH_NAME}")
            }
        }
    }
}
